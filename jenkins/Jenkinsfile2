pipeline {
    agent any

    stages {
        stage('Run Base') {
            build(job: 'test')
        }
        stage('Show Path2') {
            steps {
                sh 'pwd'
                sh 'ls -al'
            }
        }
    }
}