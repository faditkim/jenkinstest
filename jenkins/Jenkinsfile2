def testPip = load "Jenkinsfile"

pipeline {
    agent any

    stages {
        stage('Run Base') {
            steps {
//                build(job: 'test')
                testPip.test()
            }
        }
        stage('Show Path2') {
            steps {
                sh 'pwd'
                sh 'ls -al'
            }
        }
    }
}