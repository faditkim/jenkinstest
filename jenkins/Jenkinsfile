pipeline {
    agent any

    stages {
        stage('Base') {
            steps {
                sh 'echo \'Hi I am Base\''
                sh 'echo pwd'
            }
        }

        stage('Prepare') {
            steps {
                sh 'echo prepare'
            }
        }
    }
}
